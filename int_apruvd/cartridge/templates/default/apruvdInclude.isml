<iscomment>
    This template is included using hook and the purpose is to remotely include 
    scripts call and avoid caching of customer specific data
</iscomment>

<isinclude url="${URLUtils.url('Apruvd-GetScript', 'pageId', pdict.pageId, 'tag', pdict.tag)}" />